# Bug 记录

> 本目录统一记录开发过程中发现的 Bug（含设计缺陷、实现缺陷、文档与实现不一致等）。
> 每个 Bug 独立成档，开头为追踪表格，用于标记是否修复、修复版本、Git 提交等。

## 追踪总表

| Bug ID | 标题 | 状态 | 修复版本 | Git 提交 | 发现日期 |
| --- | --- | --- | --- | --- | --- |
| [BUG-000](./BUG-000_data_guard_timer.md) | 数据模式定时器未重载导致 `+++` 静默守卫失效 | 已修复 | lw_at_core.c v1.10.1 | - | 2026-08-01 |
| [BUG-001](./BUG-001_lw_at_plus_seq_cross_segment.md) | `+++` 跨段累积可退出（误触发风险） | 已修复 | lw_at v1.14.0 | 待提交 | 2026-08-11 |

---

## 说明

- **Bug ID**：`BUG-xxx`，按发现顺序递增。
- **状态**：`待修复` / `修复中` / `已修复` / `已关闭`（不再处理）。
- **修复版本**：修复对应固件/库的语义化版本（如 `v1.3.0`），未修复填 `-`。
- **Git 提交**：修复提交的 hash，未修复填 `-`。
- 每个 Bug 详情见对应文件：`BUG-xxx_<简述>.md`。
